# 에코팜 위험도 시스템 — 설계 근거 문서

**작성 목적:** 대시보드가 실시간 모델 추론 없이 정적 파일만으로 동작하는 이유를 기록한다.  
심사·리뷰 시 "왜 이렇게 설계했는가"에 대한 공식 답변 문서.

---

## 1. 문제 정의

에코팜 현장 관리자는 **구역별 위험도와 대응 매뉴얼을 즉시** 확인해야 한다.  
모델 파이프라인(LightGBM 3단계 캐스케이드 + Isotonic 보정)은 학습 완료 후 **배포 단계**에서 두 가지 방식이 가능하다.

| 방식 | 설명 |
|------|------|
| A. 실시간 추론 | 대시보드 → 모델 서버 API 호출 → 결과 반환 |
| B. 사전 계산 (채택) | 모델 실행(1회) → 정적 JSON/CSV 저장 → 대시보드가 파일 조회 |

**이 시스템은 방식 B를 채택했다.**

---

## 2. 채택 근거 — 입력 공간이 유한·이산적이다

### 2-1. 조합 수 계산

| 입력 축 | 선택지 | 개수 |
|---------|--------|------|
| 시설 (구역) | F01~F06 | 6 |
| 혼잡도 | 여유 / 보통 / 혼잡 / 매우혼잡 | 4 |
| 환경 조건 | {폭염, 우천, 한파, 강풍, 야간, 피크타임}의 부분집합 | 2⁶ = 64 |
| **전체 조합** | 6 × 4 × 64 | **1,536** |

1,536건은 일괄 계산에 약 17초 소요된다.  
대시보드 응답 시간 목표(< 100 ms) 대비 **실시간 추론 1회(~10 ms)** 는 절약 가치가 없고,  
오히려 런타임 의존성 · 네트워크 지연 · 서버 장애 위험을 추가한다.

### 2-2. 입력이 이산적인 이유

- **혼잡도:** 실제 방문객 수(연속값)를 4구간으로 양자화. 현장 관리자가 숫자를 직접 입력하는 대신 직관적 레벨로 선택. 이 양자화 자체가 UX 설계이며, 동시에 입력 공간을 닫힌 집합으로 만든다.
- **조건:** 기상·시간 조건을 6개 바이너리 플래그로 표현. 연속 기온 대신 "폭염 여부"로 분류하는 것은 현장 운영 단위(기상청 특보)와 일치한다.
- **시설:** 운영 구역은 기획 단계에서 확정된다. 신규 구역 추가는 모델 재학습 이벤트이므로 재계산도 당연히 수반된다.

---

## 3. 실시간 추론 대비 비교

| 항목 | A. 실시간 추론 | B. 사전 계산 (채택) |
|------|--------------|---------------------|
| 응답 속도 | ~10 ms (모델 호출) | < 1 ms (dict 조회) |
| 런타임 의존성 | Python + LightGBM + 모델 파일 상시 필요 | JSON 파일 하나로 완결 |
| 오프라인 동작 | 불가 | 가능 (정적 파일 서빙) |
| 결과 일관성 | 라이브러리 버전 업그레이드에 민감 | 스냅샷 고정 — 항상 동일 |
| 배포 복잡도 | 모델 서빙 인프라 필요 (Flask, FastAPI 등) | 파일 복사만으로 배포 |
| 감사(Audit) 용이성 | 로그 없으면 재현 불가 | CSV 자체가 감사 기록 |
| 현장 태블릿·키오스크 | 네트워크 필수 | 오프라인 완전 동작 |

---

## 4. 재계산 트리거

사전 계산 결과물(`ecofarm_risk_lookup.json`, `ecofarm_risk_lookup.csv`)은 아래 이벤트에서만 재생성한다.

1. **모델 재학습** — 신규 사고 데이터 추가, 피처 변경, 하이퍼파라미터 튜닝
2. **시설 구역 변경** — 신규 구역 추가, 폐쇄
3. **위험 단계 임계값 정책 변경** — 운영 정책 결정 (현재: 위험 ≥ 0.15 / 주의 ≥ 0.07 / 관찰 ≥ 0.03)

위 세 이벤트는 모두 **배치성 이벤트**이며 일상 운영 중 발생하지 않는다.

---

## 5. 저장 파일 구조

### 5-1. `ecofarm_risk_lookup.json` (2.3 MB)

```
{
  "meta": {
    "generated_at": "2026-08-16T...",
    "total": 1536,
    "facilities": [...],
    "crowd_levels": ["여유", "보통", "혼잡", "매우혼잡"],
    "condition_options": ["폭염", "우천", "한파", "강풍", "야간", "피크타임"],
    "risk_thresholds": {"위험": 0.15, "주의": 0.07, "관찰": 0.03, "안전": 0.0}
  },
  "lookup": {
    "F01": {
      "매우혼잡": {
        "우천|폭염|피크타임": {
          "risk_score": 0.1411,
          "risk_level": "주의",
          "risk_icon": "🟡",
          "p_emergency": 0.1058,
          "p_severity": {"경증": 0.7246, "중등증": 0.1719, "중증": 0.1023},
          "actions": [...],
          "staff": "예방 모니터 1명 + 대기 요원 1명",
          ...
        }
      }
    }
  }
}
```

**조회 방법 (O(1)):**
```python
entry = lookup["F01"]["매우혼잡"]["우천|폭염|피크타임"]
```
조건 키는 **정렬된 조건명을 `|`로 연결**한다. (순서 무관)

### 5-2. `ecofarm_risk_lookup.csv` (1.1 MB, 1,536행)

| 컬럼 | 설명 |
|------|------|
| facility_id, facility_name | 시설 ID 및 한글명 |
| crowd_level | 혼잡도 4단계 |
| conditions | 적용 조건 (`\|` 구분, 없으면 `없음`) |
| risk_score | 위험도 스코어 (0–1) |
| risk_level / risk_icon | 단계 텍스트 / 이모지 |
| p_emergency | Stage 1 응급 발생 확률 (보정 후) |
| p_sev_경증 / p_sev_중등증 / p_sev_중증 | Stage 3 중증도 분포 |
| staff | 필요 인력 |
| equipment | 필요 장비 |
| actions | 추천 조치 (`\|` 구분) |
| escalation | 에스컬레이션 기준 |
| fac_note | 시설 특이사항 |

---

## 6. 한계 및 대응 방안

| 한계 | 원인 | 대응 |
|------|------|------|
| 연속 변수 이산화 손실 | 기온·밀도를 구간값으로 근사 | 4구간 혼잡도로 현장 운영 단위에 맞게 충분히 근사; 정밀도 필요 시 구간 세분화 |
| 한파 모델 피처 미반영 | 학습 데이터(2025-08~2026-07) 기간 한파 0건 → 상수 피처로 제외 | 기온(-3°C)으로 간접 반영; 향후 데이터 보강 후 재학습 |
| '위험' 단계 미출현 | 모델 보정이 보수적; 최고 스코어 0.141 | 임계값 하향(예: 0.12) 검토 또는 데이터 보강 후 재학습 |
| 안전 단계 0건 | 여름 휴일 기준 설정 — 낮은 밀도도 관찰 이상 | 평일·비수기 시나리오 파라미터화 가능 |

---

## 7. 결론

> **이 시스템은 "추론 엔진"이 아니라 "정책 테이블"이다.**

모델은 오프라인에서 한 번 실행되고, 현장 대시보드는 JSON 파일 하나만 읽으면 된다.  
추가 인프라 없이 태블릿·키오스크·오프라인 환경에 즉시 배포 가능하다.  
이것이 **1,536개 유한 입력 공간**을 활용한 사전 계산 설계의 핵심 강점이다.
